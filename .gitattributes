# Treat everything as text by default
* text=auto eol=lf

# Treat SVGs as text
*.svg text eol=lf

# Optional: force JS, CSS, HTML, JSON, Markdown to LF
*.js text eol=lf
*.css text eol=lf
*.html text eol=lf
*.json text eol=lf
*.md text eol=lf
